wget https://bitbucket.org/pypa/setuptools/raw/0.7.7/ez_setup.py -O - | python

url -O https://raw.github.com/pypa/pip/master/contrib/get-pip.py
$ [sudo] python get-pip.py

sudo pip install Django


apt-get install libcurl4-gnutls-dev libexpat1-dev gettext \
  libz-dev libssl-dev

apt-get install git

But first you want to make sure you cd into your .ssh directory. Open up the terminal and run cd ~/.ssh && ssh-keygen
next you need to copy this to your clipboard. On OSX run cat id_rsa.pub | pbcopy on Linux run cat id_rsa.pub | xclip
Add your key to your account via the website.
finally setup your git config
git config --global user.name "bob"
git config --global user.email bob@... (don't forget to restart your command line to make sure the config is reloaded)
Thats it you should be good to clone and checkout

apt-get install libapache2-mod-wsgi

#dont go in root


#vhost.conf
WSGIScriptAlias /learned /home/codes/learned/learned/wsgi.py
WSGIPythonPath /home/codes/learned/

<Directory /home/codes/learned/learned>
<Files wsgi.py>
    Order allow,deny
    Allow from all
</Files>
</Directory>

static_root = "/var/www/djano_learned"
static_url = "djano_learned"

apt-get install python-beautifulsoup , python-requests


pip install django-tinymce
